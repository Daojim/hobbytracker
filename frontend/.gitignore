# Vite build output and caches. node_modules is already covered by the root .gitignore.
dist/
node_modules/
*.local
.vite/
*.tsbuildinfo

# Playwright artefacts. The browsers themselves live outside the repo.
playwright-report/
test-results/
blob-report/
.playwright/
